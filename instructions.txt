to create virtualenv on this machine:

pip install --user virtualenv
export PATH=$PATH:~/.local/bin
virtualenv snac-env
source snac-env/bin/activate

Then run the following code to check for gpus
import torch

if torch.cuda.is_available():
    print("PyTorch can use GPUs!")
else:
    print("PyTorch cannot use GPUs.")

To run snac_evaluate:
preprocess using snac-preprocess.ipynb to get a .tsv file
decide to preprocess sentence-wise or in multi-sentence pieces 
then ran snac_evauate
python3 snac_evaluate.py --validation_file output.tsv --model_name_or_path  t5-span   --output_dir test-outputs-snac
then visualize the output with html tags like in the paper

for qafacteval